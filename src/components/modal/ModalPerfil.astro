---
import Box from "../icons/Box.astro";
---

<!-- Button trigger modal for Box -->
<button type="button" data-bs-toggle="modal" data-bs-target="#boxModal">
    <Box />
</button>

<!-- Custom Background for Box Modal -->
<div id="box-backdrop" class="fixed inset-0 bg-gray-900/50 z-40 hidden"></div>

<!-- Modal for Box -->
<div
    class="modal fade"
    id="boxModal"
    data-bs-backdrop="false"
    data-bs-keyboard="false"
    tabindex="-1"
    aria-labelledby="boxModalLabel"
    aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content font-font-cust-2">
            <div class="flex justify-end px-2 py-1">
                <button
                    type="button"
                    class="btn-close"
                    data-bs-dismiss="modal"
                    aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <h2 class="font-semibold text-center text-2xl">
                    Se requiere iniciar sesión
                </h2>
                <h3 class="text-center">
                    Tienes que iniciar sesión para utilizar esta función.
                </h3>
                <hr class="my-2" />
                <p class="text-wrap text-sm mx-auto text-black">
                    La seccion a la que va a acceder contiene información
                    personal, por lo que necesitamos su identidad para saber qué
                    información podemos proporcionarle. Por lo tanto, deberá
                    iniciar sesión rigistrarse si realmente desea acceder.
                </p>
                <div
                    class="w-full flex flex-col mx-auto justify-center items-center gap-2 mt-3">
                    <button
                        class="text-black bg-[#ebebeb] w-full rounded-md py-2"
                        type="button"
                        class="btn-close"
                        data-bs-dismiss="modal"
                        aria-label="Close">Ir a inicar sesion</button
                    >
                    <button
                        class="text-white w-full rounded-md py-2 bg-blue-600"
                        >Quiero registrarme</button
                    >
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    /* Personalizar el fondo del modal para Box */
    #box-backdrop {
        display: none; /* Ocultar por defecto */
    }

    #boxModal.show + #box-backdrop {
        display: block; /* Mostrar cuando el modal esté activo */
    }

    .modal {
        z-index: 1059; /* Z-index de Bootstrap para el modal por defecto */
    }
</style>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        var boxModalEl = document.getElementById("boxModal");

        if (boxModalEl) {
            boxModalEl.addEventListener("show.bs.modal", function (event) {
                var boxBackdrop = document.getElementById("box-backdrop");
                if (boxBackdrop) {
                    boxBackdrop.style.display = "block";
                }
            });

            boxModalEl.addEventListener("hide.bs.modal", function (event) {
                var boxBackdrop = document.getElementById("box-backdrop");
                if (boxBackdrop) {
                    boxBackdrop.style.display = "none";
                }
            });
        }
    });
</script>
